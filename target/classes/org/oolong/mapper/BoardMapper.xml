<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.oolong.mapper.BoardMapper">
	<insert id="insert">
		<selectKey order="AFTER" keyProperty="bno" resultType="long">
			select LAST_INSERT_ID()
		</selectKey>
		insert into tbl_board(title, content, writer) values(#{title}, #{content}, #{writer})
	</insert>
	
	<select id="selectOne" resultType="BoardDTO">
		select * from tbl_board where bno = #{bno}
	</select>
	
	<update id="remove">
		update tbl_board set delflag = true where bno = #{bno}
	</update>
	
	<update id="update">
		update tbl_board
			set title = #{title}, content = #{content}, updatedate = now(), delflag = #{delFlag}
		where bno = #{bno}
	</update>
	
	<select id="list" resultType="BoardDTO">
		select bno, title, writer, regdate from tbl_board
		where delflag = false
		order by bno desc
	</select>
	
	
</mapper>