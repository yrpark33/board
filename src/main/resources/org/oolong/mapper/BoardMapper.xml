<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.oolong.mapper.BoardMapper">
	<insert id="insert">
		
		<selectKey order="AFTER" keyProperty="boardId" resultType="long">
			SELECT LAST_INSERT_ID()
		</selectKey>
		
		insert into tbl_board (title, content, writer)
		values (#{title}, #{content}, #{writer})
	</insert>
	
	<select id="selectById" resultType="BoardDTO">
		select * from tbl_board where board_id = #{boardId}
	</select>
	
	<update id="delete">
		update tbl_board
			set is_deleted = true, deleted_at = now()
		where board_id = #{boardId}
	</update>
	
	<update id="update">
		update tbl_board
			set title = #{title}, content = #{content}, updated_at = now()
		where board_id = #{boardId}
	</update>
	
	<select id="selectList">
		select board_id, title, writer, created_at from tbl_board where is_deleted = false order by board_id desc
	</select>
	
	
</mapper>