<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.oolong.mapper.BoardMapper">
	<insert id="insert">
		
		<selectKey order="AFTER" keyProperty="boardId" resultType="long">
			SELECT LAST_INSERT_ID()
		</selectKey>
		
		INSERT INTO tbl_board (title, content, writer)
		VALUES (#{title}, #{content}, #{writer})
	</insert>
	
	<select id="selectById" resultType="BoardDTO">
		SELECT * FROM tbl_board
		WHERE board_id = #{boardId} AND deleted = FALSE
	</select>
	
	<update id="delete">
		UPDATE tbl_board
			SET deleted = TRUE, deleted_at = NOW()
		WHERE board_id = #{boardId} AND deleted = FALSE
	</update>
	
	<update id="update">
		UPDATE tbl_board
			SET title = #{title}, content = #{content}, updated_at = NOW()
		WHERE board_id = #{boardId} AND deleted = FALSE
	</update>
	
	<sql id="search">
		<if test="types != null and keyword != null">
			<foreach collection="types" item="type" open="AND (" close=")" separator=" OR ">
				<if test='type.equals("T")'>
					b.title LIKE CONCAT('%', #{keyword}, '%')
				</if>
				<if test='type.equals("C")'>
					b.content LIKE CONCAT('%', #{keyword), '%')
				</if>
				<if test='type.equals("W")'>
					b.writer LIKE CONCAT('%', #{keyword}, '%')
				</if>
			</foreach>
		</if>
	</sql>
	
	<select id="selectTotalCountSearch" resultType="int">
		SELECT COUNT(*) FROM tbl_board b
		
		WHERE deleted = FALSE
		
		<include refid="search"></include>
		
		
	</select>
	
	<select id="selectListSearch" resultType="BoardDTO">
		SELECT b.board_id, title, b.writer, b.created_at, count(comment_id) AS commentCount
		FROM tbl_board b
		LEFT JOIN tbl_comment c
		ON b.board_id = c.board_id
		WHERE b.deleted = FALSE
		
		<include refid="search"></include>
		
		GROUP BY b.board_id
		ORDER BY b.board_id DESC
		LIMIT #{limit} OFFSET #{offset}
		
	</select>
	
	
</mapper>